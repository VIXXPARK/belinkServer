language: node_js
node_js:
  - 12

sudo: true

services:
  - mysql

branches:
  only:
    - main

cache:
  directories:
    - node_modules

env:
  - NODE_ENV=test

before_install:
  - npm install -g
  - npm install mocha chai chai-http express jsonwebtoken sequelize dotenv
  - npm install -g mysql2
  - mysql -e 'create database belink_test;'
  - mysql -e 'create database belink;'
  - mysql -e 'grant all privileges on belink_test.* to travis@localhost identified by"";'
  
install:



before_scripts:
  
  

script:
  - npm test
