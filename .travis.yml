language: node_js
node_js:
  - "node"

services:
  - mysql

branches:
  only:
    - main

cache:
  directories:
    - node_modules

env:
  - NODE_ENV=test

install:
  - sudo npm uninstall node-gyp -g
  - npm uninstall node-gyp
  - npm install

script:
  - npm test
